local Library = {}
local TweenService = game:GetService("TweenService")
local UIS = game:GetService("UserInputService")
local player = game.Players.LocalPlayer
local Players = game:GetService("Players")

function Library:CreateWindow(title, imageId)

	local gui = Instance.new("ScreenGui")
	gui.Parent = player:WaitForChild("PlayerGui")
	gui.IgnoreGuiInset = true
	gui.ResetOnSpawn = false

	-- LOGIN
	local Login = Instance.new("Frame")
	Login.Size = UDim2.new(1,0,1,0)
	Login.BackgroundColor3 = Color3.fromRGB(0,0,0)
	Login.BackgroundTransparency = 0.5
	Login.Parent = gui

	local loginText = Instance.new("TextLabel")
	loginText.Size = UDim2.new(1,0,1,0)
	loginText.Text = "Loading..."
	loginText.TextColor3 = Color3.fromRGB(255,128,0)
	loginText.BackgroundTransparency = 1
	loginText.TextScaled = true
	loginText.Parent = Login

	task.wait(2)
	Login:Destroy()

	local notified = false

	local function Notify(titulo, texto, duracao, icon)
		pcall(function()
			game:GetService("StarterGui"):SetCore("SendNotification", {
				Title = titulo,
				Text = texto,
				Duration = duracao or 5,
				Icon = icon and ("rbxassetid://"..icon) or nil
			})
		end)
	end
	Library.Notify = Notify

	local function ApplyFont(obj)
		if obj:IsA("TextLabel") or obj:IsA("TextButton") then
			obj.Font = Enum.Font.BuilderSans
			obj.TextSize = 12
		end
	end

	-- BOTÃO TOGGLE
	local Toggle = Instance.new("ImageButton")
	Toggle.Size = UDim2.new(0,40,0,40)
	Toggle.Position = UDim2.new(0,10,0.5,-20)
	Toggle.BackgroundColor3 = Color3.fromRGB(255,128,0)
	Toggle.Image = "rbxassetid://97939372251102"
	Toggle.Parent = gui
	Instance.new("UICorner", Toggle).CornerRadius = UDim.new(1,0)

	local draggingBtn, dragStartBtn, startPosBtn

	Toggle.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
			draggingBtn = true
			dragStartBtn = input.Position
			startPosBtn = Toggle.Position
		end
	end)

	Toggle.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
			draggingBtn = false
		end
	end)

	UIS.InputChanged:Connect(function(input)
		if draggingBtn and (input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseMovement) then
			local delta = input.Position - dragStartBtn
			Toggle.Position = UDim2.new(
				startPosBtn.X.Scale,
				startPosBtn.X.Offset + delta.X,
				startPosBtn.Y.Scale,
				startPosBtn.Y.Offset + delta.Y
			)
		end
	end)

	-- MAIN - MAIS LARGO E MAIS BAIXO (DEITADO)
	local Main = Instance.new("Frame")
	Main.Size = UDim2.new(0,450,0,350)  -- 450 largura x 350 altura (formato mais horizontal)
	Main.Position = UDim2.new(0.5,-225,0.5,-175)
	Main.BackgroundColor3 = Color3.fromRGB(0,0,0)
	Main.BackgroundTransparency = 0.2
	Main.Parent = gui
	Instance.new("UICorner", Main).CornerRadius = UDim.new(0,8)

	local draggingMain, dragStart, startPos

	Main.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
			draggingMain = true
			dragStart = input.Position
			startPos = Main.Position
		end
	end)

	Main.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
			draggingMain = false
		end
	end)

	UIS.InputChanged:Connect(function(input)
		if draggingMain and (input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseMovement) then
			local delta = input.Position - dragStart
			Main.Position = UDim2.new(
				startPos.X.Scale,
				startPos.X.Offset + delta.X,
				startPos.Y.Scale,
				startPos.Y.Offset + delta.Y
			)
		end
	end)

	local strokeMain = Instance.new("UIStroke")
	strokeMain.Color = Color3.fromRGB(255,128,0)
	strokeMain.Thickness = 1
	strokeMain.Parent = Main

	-- SIDEBAR
	local Sidebar = Instance.new("Frame")
	Sidebar.Size = UDim2.new(0,90,1,0)
	Sidebar.BackgroundColor3 = Color3.fromRGB(20,20,20)
	Sidebar.BackgroundTransparency = 0.2
	Sidebar.Parent = Main
	Instance.new("UICorner", Sidebar).CornerRadius = UDim.new(0,8)

	local strokeSidebar = Instance.new("UIStroke")
	strokeSidebar.Color = Color3.fromRGB(255,128,0)
	strokeSidebar.Parent = Sidebar

	local Title = Instance.new("TextLabel")
	Title.Size = UDim2.new(1,0,0,30)
	Title.Text = title
	Title.TextColor3 = Color3.fromRGB(255,128,0)
	Title.BackgroundTransparency = 1
	Title.TextSize = 14
	Title.Font = Enum.Font.BuilderSans
	Title.Parent = Sidebar

	local Content = Instance.new("Frame")
	Content.Size = UDim2.new(1,-90,1,0)
	Content.Position = UDim2.new(0,90,0,0)
	Content.BackgroundTransparency = 1
	Content.Parent = Main

	local currentTab
	local tabY = 35

	function Library:CreateTab(name)
		local btn = Instance.new("TextButton")
		btn.Size = UDim2.new(1,-10,0,28)
		btn.Position = UDim2.new(0,5,0,tabY)
		btn.Text = name
		btn.BackgroundColor3 = Color3.fromRGB(40,40,40)
		btn.TextColor3 = Color3.fromRGB(255,128,0)
		btn.Font = Enum.Font.BuilderSans
		btn.TextSize = 12
		btn.Parent = Sidebar
		Instance.new("UICorner", btn).CornerRadius = UDim.new(0,4)

		tabY += 33

		local Tab = Instance.new("ScrollingFrame")
		Tab.Size = UDim2.new(1,0,1,0)
		Tab.Visible = false
		Tab.Parent = Content
		Tab.ScrollBarThickness = 3
		Tab.CanvasSize = UDim2.new(0,0,0,0)
		Tab.AutomaticCanvasSize = Enum.AutomaticSize.Y
		Tab.BackgroundTransparency = 1

		local layout = Instance.new("UIListLayout", Tab)
		layout.Padding = UDim.new(0,6)
		layout.SortOrder = Enum.SortOrder.LayoutOrder

		btn.MouseButton1Click:Connect(function()
			if currentTab then currentTab.Visible = false end
			currentTab = Tab
			Tab.Visible = true
		end)

		local Elements = {}

		function Elements:AddSection(titleText)
			local Section = {}

			local sectionFrame = Instance.new("Frame")
			sectionFrame.Size = UDim2.new(1,-10,0,0)
			sectionFrame.BackgroundColor3 = Color3.fromRGB(15,15,15)
			sectionFrame.BackgroundTransparency = 0.3
			sectionFrame.Parent = Tab
			Instance.new("UICorner", sectionFrame).CornerRadius = UDim.new(0,6)

			local stroke = Instance.new("UIStroke", sectionFrame)
			stroke.Color = Color3.fromRGB(255,128,0)
			stroke.Thickness = 1

			local sectionLayout = Instance.new("UIListLayout", sectionFrame)
			sectionLayout.Padding = UDim.new(0,4)

			local title = Instance.new("TextLabel")
			title.Size = UDim2.new(1,0,0,22)
			title.Text = "  "..titleText
			title.BackgroundTransparency = 1
			title.TextColor3 = Color3.fromRGB(255,128,0)
			title.TextSize = 12
			title.Font = Enum.Font.BuilderSans
			title.TextXAlignment = Enum.TextXAlignment.Left
			title.Parent = sectionFrame

			local container = Instance.new("Frame")
			container.Size = UDim2.new(1,0,0,0)
			container.BackgroundTransparency = 1
			container.Parent = sectionFrame

			local clayout = Instance.new("UIListLayout", container)
			clayout.Padding = UDim.new(0,4)
			clayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

			-- Função para atualizar altura do container
			local function updateContainerHeight()
				local totalHeight = 0
				for _, child in ipairs(container:GetChildren()) do
					if child:IsA("Frame") or child:IsA("TextButton") then
						totalHeight = totalHeight + child.Size.Y.Offset + 4
					end
				end
				container.Size = UDim2.new(1,0,0,totalHeight)
				sectionFrame.Size = UDim2.new(1,-10,0,22 + totalHeight + 8)
			end

			function Section:AddLabel(text)
				local l = Instance.new("TextLabel")
				l.Size = UDim2.new(1,-10,0,18)
				l.Text = text
				l.BackgroundTransparency = 1
				l.TextColor3 = Color3.fromRGB(200,200,200)
				l.Font = Enum.Font.BuilderSans
				l.TextSize = 11
				l.Parent = container
				task.wait()
				updateContainerHeight()
			end

			function Section:AddButton(text, callback)
				local b = Instance.new("TextButton")
				b.Size = UDim2.new(1,-10,0,28)
				b.Text = text
				b.BackgroundColor3 = Color3.fromRGB(30,30,30)
				b.TextColor3 = Color3.fromRGB(255,128,0)
				b.Font = Enum.Font.BuilderSans
				b.TextSize = 12
				b.Parent = container
				Instance.new("UICorner", b).CornerRadius = UDim.new(0,4)
				b.MouseButton1Click:Connect(callback)
				task.wait()
				updateContainerHeight()
			end

			function Section:AddRetrato()
				local frame = Instance.new("Frame")
				frame.Size = UDim2.new(1,-10,0,140)
				frame.BackgroundColor3 = Color3.fromRGB(20,20,20)
				frame.Parent = container
				Instance.new("UICorner", frame).CornerRadius = UDim.new(0,6)

				local stroke = Instance.new("UIStroke", frame)
				stroke.Color = Color3.fromRGB(255,128,0)
				stroke.Thickness = 1

				local viewport = Instance.new("ViewportFrame")
				viewport.Size = UDim2.new(1,-20,1,-20)
				viewport.Position = UDim2.new(0,10,0,10)
				viewport.BackgroundColor3 = Color3.fromRGB(0,0,0)
				viewport.Parent = frame
				Instance.new("UICorner", viewport).CornerRadius = UDim.new(0,4)

				local character = player.Character or player.CharacterAdded:Wait()

				local characterClone = character:Clone()
				characterClone.Parent = viewport
				characterClone:SetPrimaryPartCFrame(CFrame.new(0, -3, 6))

				local camera = Instance.new("Camera")
				camera.Parent = viewport
				viewport.CurrentCamera = camera
				camera.CFrame = CFrame.new(0, 0, 10)

				local light = Instance.new("PointLight")
				light.Parent = characterClone
				light.Color = Color3.fromRGB(255,128,0)
				light.Range = 15
				light.Brightness = 2

				player.CharacterAdded:Connect(function(newChar)
					characterClone:Destroy()
					local newClone = newChar:Clone()
					newClone.Parent = viewport
					newClone:SetPrimaryPartCFrame(CFrame.new(0, -3, 6))
					light.Parent = newClone
				end)

				task.wait()
				updateContainerHeight()
			end

			function Section:AddNamePlayer()
				local frame = Instance.new("Frame")
				frame.Size = UDim2.new(1,-10,0,60)
				frame.BackgroundColor3 = Color3.fromRGB(20,20,20)
				frame.Parent = container
				Instance.new("UICorner", frame).CornerRadius = UDim.new(0,6)

				local stroke = Instance.new("UIStroke", frame)
				stroke.Color = Color3.fromRGB(255,128,0)
				stroke.Thickness = 1

				local nameLabel = Instance.new("TextLabel")
				nameLabel.Size = UDim2.new(1,-20,0,25)
				nameLabel.Position = UDim2.new(0,10,0,5)
				nameLabel.Text = "👤 " .. player.Name
				nameLabel.BackgroundTransparency = 1
				nameLabel.TextColor3 = Color3.fromRGB(255,128,0)
				nameLabel.TextSize = 12
				nameLabel.Font = Enum.Font.BuilderSans
				nameLabel.TextXAlignment = Enum.TextXAlignment.Left
				nameLabel.Parent = frame

				local copyBtn = Instance.new("TextButton")
				copyBtn.Size = UDim2.new(1,-20,0,22)
				copyBtn.Position = UDim2.new(0,10,0,32)
				copyBtn.Text = "📋 Copiar Nome"
				copyBtn.BackgroundColor3 = Color3.fromRGB(30,30,30)
				copyBtn.TextColor3 = Color3.fromRGB(255,128,0)
				copyBtn.Font = Enum.Font.BuilderSans
				copyBtn.TextSize = 11
				copyBtn.Parent = frame
				Instance.new("UICorner", copyBtn).CornerRadius = UDim.new(0,4)

				copyBtn.MouseButton1Click:Connect(function()
					local clipboard = game:GetService("Clipboard")
					clipboard:set(player.Name)
					Notify("Copiado!", "Nome copiado: " .. player.Name, 2)
				end)

				task.wait()
				updateContainerHeight()
			end

			function Section:AddPlayersServer()
				local frame = Instance.new("Frame")
				frame.Size = UDim2.new(1,-10,0,60)
				frame.BackgroundColor3 = Color3.fromRGB(20,20,20)
				frame.Parent = container
				Instance.new("UICorner", frame).CornerRadius = UDim.new(0,6)

				local stroke = Instance.new("UIStroke", frame)
				stroke.Color = Color3.fromRGB(255,128,0)
				stroke.Thickness = 1

				local playersCount = Instance.new("TextLabel")
				playersCount.Size = UDim2.new(1,-20,0,25)
				playersCount.Position = UDim2.new(0,10,0,5)
				playersCount.Text = "👥 Players: " .. #Players:GetPlayers()
				playersCount.BackgroundTransparency = 1
				playersCount.TextColor3 = Color3.fromRGB(255,128,0)
				playersCount.TextSize = 12
				playersCount.Font = Enum.Font.BuilderSans
				playersCount.TextXAlignment = Enum.TextXAlignment.Left
				playersCount.Parent = frame

				local refreshBtn = Instance.new("TextButton")
				refreshBtn.Size = UDim2.new(1,-20,0,22)
				refreshBtn.Position = UDim2.new(0,10,0,32)
				refreshBtn.Text = "🔄 Atualizar"
				refreshBtn.BackgroundColor3 = Color3.fromRGB(30,30,30)
				refreshBtn.TextColor3 = Color3.fromRGB(255,128,0)
				refreshBtn.Font = Enum.Font.BuilderSans
				refreshBtn.TextSize = 11
				refreshBtn.Parent = frame
				Instance.new("UICorner", refreshBtn).CornerRadius = UDim.new(0,4)

				local function updateCount()
					playersCount.Text = "👥 Players: " .. #Players:GetPlayers()
				end

				refreshBtn.MouseButton1Click:Connect(updateCount)
				Players.PlayerAdded:Connect(updateCount)
				Players.PlayerRemoving:Connect(updateCount)

				task.wait()
				updateContainerHeight()
			end

			function Section:AddDiscordInvite(serverName, inviteLink, imageAssetId)
				local frame = Instance.new("Frame")
				frame.Size = UDim2.new(1,-10,0,95)
				frame.BackgroundColor3 = Color3.fromRGB(20,20,20)
				frame.Parent = container
				Instance.new("UICorner", frame).CornerRadius = UDim.new(0,6)

				local stroke = Instance.new("UIStroke", frame)
				stroke.Color = Color3.fromRGB(255,128,0)
				stroke.Thickness = 1

				if imageAssetId then
					local image = Instance.new("ImageLabel")
					image.Size = UDim2.new(0,45,0,45)
					image.Position = UDim2.new(0,10,0,10)
					image.Image = "rbxassetid://" .. imageAssetId
					image.BackgroundTransparency = 1
					image.Parent = frame
					Instance.new("UICorner", image).CornerRadius = UDim.new(1,0)
				end

				local nameLabel = Instance.new("TextLabel")
				nameLabel.Size = UDim2.new(1,-80,0,25)
				nameLabel.Position = UDim2.new(0,65,0,8)
				nameLabel.Text = serverName
				nameLabel.BackgroundTransparency = 1
				nameLabel.TextColor3 = Color3.fromRGB(255,128,0)
				nameLabel.TextSize = 12
				nameLabel.Font = Enum.Font.BuilderSans
				nameLabel.TextXAlignment = Enum.TextXAlignment.Left
				nameLabel.Parent = frame

				local inviteText = Instance.new("TextLabel")
				inviteText.Size = UDim2.new(1,-80,0,18)
				inviteText.Position = UDim2.new(0,65,0,33)
				inviteText.Text = inviteLink
				inviteText.BackgroundTransparency = 1
				inviteText.TextColor3 = Color3.fromRGB(150,150,150)
				inviteText.TextXAlignment = Enum.TextXAlignment.Left
				inviteText.TextSize = 10
				inviteText.Font = Enum.Font.BuilderSans
				inviteText.Parent = frame

				local joinBtn = Instance.new("TextButton")
				joinBtn.Size = UDim2.new(0,70,0,28)
				joinBtn.Position = UDim2.new(1,-80,1,-35)
				joinBtn.Text = "JOIN"
				joinBtn.BackgroundColor3 = Color3.fromRGB(88,101,242)
				joinBtn.TextColor3 = Color3.fromRGB(255,255,255)
				joinBtn.Font = Enum.Font.BuilderSans
				joinBtn.TextSize = 11
				joinBtn.Parent = frame
				Instance.new("UICorner", joinBtn).CornerRadius = UDim.new(0,4)

				joinBtn.MouseButton1Click:Connect(function()
					local clipboard = game:GetService("Clipboard")
					clipboard:set(inviteLink)
					Notify("Discord Invite", "Link copiado: " .. inviteLink, 3)
				end)

				task.wait()
				updateContainerHeight()
			end

			function Section:AddDropdown(text, options, callback)
				local open = false

				local main = Instance.new("TextButton")
				main.Size = UDim2.new(1,-10,0,28)
				main.Text = text
				main.BackgroundColor3 = Color3.fromRGB(30,30,30)
				main.TextColor3 = Color3.fromRGB(255,128,0)
				main.Font = Enum.Font.BuilderSans
				main.TextSize = 12
				main.Parent = container
				Instance.new("UICorner", main).CornerRadius = UDim.new(0,4)

				local list = Instance.new("Frame")
				list.Size = UDim2.new(1,-10,0,0)
				list.ClipsDescendants = true
				list.BackgroundColor3 = Color3.fromRGB(25,25,25)
				list.Parent = container
				Instance.new("UICorner", list).CornerRadius = UDim.new(0,4)

				local listLayout = Instance.new("UIListLayout", list)
				listLayout.Padding = UDim.new(0,2)

				for _,v in ipairs(options) do
					local opt = Instance.new("TextButton")
					opt.Size = UDim2.new(1,0,0,24)
					opt.Text = v
					opt.BackgroundTransparency = 1
					opt.TextColor3 = Color3.fromRGB(255,128,0)
					opt.Font = Enum.Font.BuilderSans
					opt.TextSize = 11
					opt.Parent = list

					opt.MouseButton1Click:Connect(function()
						main.Text = v
						open = false
						TweenService:Create(list, TweenInfo.new(0.25), {
							Size = UDim2.new(1,-10,0,0)
						}):Play()
						callback(v)
					end)
				end

				main.MouseButton1Click:Connect(function()
					open = not open
					TweenService:Create(list, TweenInfo.new(0.25), {
						Size = open and UDim2.new(1,-10,0,#options*26) or UDim2.new(1,-10,0,0)
					}):Play()
				end)

				task.wait()
				updateContainerHeight()
			end

			function Section:AddSlider(text, min, max, callback)
				local value = min
				local dragging = false

				local frame = Instance.new("Frame")
				frame.Size = UDim2.new(1,-10,0,48)
				frame.BackgroundColor3 = Color3.fromRGB(30,30,30)
				frame.Parent = container
				Instance.new("UICorner", frame).CornerRadius = UDim.new(0,4)

				local label = Instance.new("TextLabel")
				label.Size = UDim2.new(1,-10,0,18)
				label.Position = UDim2.new(0,5,0,3)
				label.Text = text.." ("..value..")"
				label.BackgroundTransparency = 1
				label.TextColor3 = Color3.fromRGB(255,128,0)
				label.Font = Enum.Font.BuilderSans
				label.TextSize = 11
				label.Parent = frame

				local bar = Instance.new("Frame")
				bar.Size = UDim2.new(1,-20,0,4)
				bar.Position = UDim2.new(0,10,1,-12)
				bar.BackgroundColor3 = Color3.fromRGB(50,50,50)
				bar.Parent = frame
				Instance.new("UICorner", bar).CornerRadius = UDim.new(1,0)

				local fill = Instance.new("Frame")
				fill.Size = UDim2.new(0,0,1,0)
				fill.BackgroundColor3 = Color3.fromRGB(255,128,0)
				fill.Parent = bar
				Instance.new("UICorner", fill).CornerRadius = UDim.new(1,0)

				local function update(input)
					local pos = math.clamp((input.Position.X - bar.AbsolutePosition.X)/bar.AbsoluteSize.X,0,1)
					fill.Size = UDim2.new(pos,0,1,0)
					value = math.floor(min + (max-min)*pos)
					label.Text = text.." ("..value..")"
					callback(value)
				end

				bar.InputBegan:Connect(function(input)
					if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
						dragging = true
						update(input)
					end
				end)

				UIS.InputChanged:Connect(function(input)
					if dragging and (input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseMovement) then
						update(input)
					end
				end)

				UIS.InputEnded:Connect(function(input)
					if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
						dragging = false
					end
				end)

				task.wait()
				updateContainerHeight()
			end

			function Section:AddToggle(text, callback)
				local state = false

				local f = Instance.new("Frame")
				f.Size = UDim2.new(1,-10,0,32)
				f.BackgroundColor3 = Color3.fromRGB(30,30,30)
				f.Parent = container
				Instance.new("UICorner", f).CornerRadius = UDim.new(0,4)

				local label = Instance.new("TextLabel")
				label.Size = UDim2.new(1,-50,1,0)
				label.Position = UDim2.new(0,10,0,0)
				label.Text = text
				label.BackgroundTransparency = 1
				label.TextColor3 = Color3.fromRGB(255,128,0)
				label.Font = Enum.Font.BuilderSans
				label.TextSize = 12
				label.TextXAlignment = Enum.TextXAlignment.Left
				label.Parent = f

				local toggle = Instance.new("Frame")
				toggle.Size = UDim2.new(0,36,0,18)
				toggle.Position = UDim2.new(1,-46,0.5,-9)
				toggle.BackgroundColor3 = Color3.fromRGB(50,50,50)
				toggle.Parent = f
				Instance.new("UICorner", toggle).CornerRadius = UDim.new(1,0)

				local circle = Instance.new("Frame")
				circle.Size = UDim2.new(0,14,0,14)
				circle.Position = UDim2.new(0,2,0.5,-7)
				circle.BackgroundColor3 = Color3.fromRGB(255,128,0)
				circle.Parent = toggle
				Instance.new("UICorner", circle).CornerRadius = UDim.new(1,0)

				f.InputBegan:Connect(function(input)
					if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
						state = not state

						local bgColor = state and Color3.fromRGB(255,128,0) or Color3.fromRGB(50,50,50)
						TweenService:Create(toggle, TweenInfo.new(0.2), {BackgroundColor3 = bgColor}):Play()
						TweenService:Create(circle, TweenInfo.new(0.2), {
							Position = state and UDim2.new(1,-16,0.5,-7) or UDim2.new(0,2,0.5,-7)
						}):Play()

						callback(state)
					end
				end)

				task.wait()
				updateContainerHeight()
			end

			task.wait()
			return Section
		end

		return Elements
	end

	-- OPEN/CLOSE
	local open = true
	Toggle.MouseButton1Click:Connect(function()
		open = not open
		Main.Visible = open

		if open and not notified then
			Notify("Welcome Sunset 🌞", "Thank you for using Sunset UI library 🌞", 5)
			notified = true
		end
	end)

	-- Abrir a primeira tab automaticamente se existir
	task.wait(0.1)
	for _, btn in ipairs(Sidebar:GetChildren()) do
		if btn:IsA("TextButton") then
			btn.MouseButton1Click:Fire()
			break
		end
	end

	return Library
end

return Library